//! CustosDB operator CLI (`custos`).
//!
//! Placeholder entry point. Operator commands (cluster status, tablet map
//! inspection, membership changes) are added in later milestones.

fn main() {
    println!(
        "custos {} — operator CLI (not implemented yet)",
        env!("CARGO_PKG_VERSION")
    );
}
