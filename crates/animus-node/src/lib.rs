//! AnimusDB's `E: Env`-generic node core (ADR 0061 Decision 1, Phase C
//! carve-out). See this crate's own `CLAUDE.md` for the full rationale.
//!
//! This is the crate skeleton (ADR 0061 rung C1, first commit): the
//! manifest boundary only. Real content lands in the commits that follow.
