# A fourth, distinct D8 test-harness bug — the poll itself asserting an unguaranteed property, not a race in production — see "A converged-or- timeout poll must target a tablet the property actually holds for" below (2026-09-03, issue #580).

**A fourth, distinct D8 test-harness bug — the poll itself asserting an
unguaranteed property, not a race in production — see "A converged-or-
timeout poll must target a tablet the property actually holds for"
below (2026-09-03, issue #580).**
