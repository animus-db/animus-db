# Architecture Decision Records

Short documents capturing significant architectural decisions: their context,
the decision, and the consequences we knowingly accept. New records use
[`0000-template.md`](0000-template.md). When a decision changes, update the
relevant ADR (or supersede it) in the same PR as the code change.

| ADR | Title | Status |
|-----|-------|--------|
| [0001](0001-two-plane-architecture.md) | Masterless AP data plane + Raft control plane | Accepted |
| [0002](0002-tablets-unit-of-placement.md) | Tablets as the unit of placement and migration | Accepted |
| [0003](0003-deterministic-simulation.md) | Deterministic simulation testing and the `Env` seam | Accepted |
| [0004](0004-dynamo-storage-primitive.md) | Dynamo-lineage storage primitive (partitioned sorted map-of-maps) | Accepted |
| [0005](0005-placement-residency.md) | Placement groups + topology-aware data residency | Accepted |
| [0006](0006-dual-cql-dynamo-adapters.md) | Dual CQL + DynamoDB adapters over a common core | Superseded by [0053](0053-dynamodb-only-drop-cql.md) |
| [0007](0007-agpl-cla.md) | AGPL-3.0 + CLA | Accepted |
| [0008](0008-borrowed-storage-first.md) | Borrowed storage engine first, custom LSM deferred | Accepted |
| [0009](0009-in-house-raft-over-env.md) | In-house Raft over the `Env` seam (deviation from openraft) | Accepted |
| [0010](0010-ap-repair-anti-entropy.md) | AP repair: read-repair + background anti-entropy | Accepted |
| [0011](0011-accord-consensus.md) | Accord-style leaderless transaction consensus (first minimal slice) | **Superseded by [0019](0019-cp-only-v1-defer-ap.md)** (2026-08-23 amendment) — `animus-consensus` deleted; rejected for CP by 0018, deferred with AP by 0019, and AP is unselectable once CQL is dropped |
| [0012](0012-failure-detection.md) | Heartbeat-based failure detection in the control plane | Accepted |
| [0013](0013-replicated-schemas.md) | Replicated table-schema catalog in the control plane | Accepted |
| [0014](0014-elle-accord-scenario-corpus.md) | Elle consistency testing against Accord + a frozen scenario corpus | **Superseded by [0019](0019-cp-only-v1-defer-ap.md)** (2026-08-23 amendment) — the Accord corpus is deleted; the Elle *checkers* live on under the ADR 0017/0018 corpora |
| [0015](0015-observability.md) | Deterministic-safe observability seam (metrics) | Accepted |
| [0016](0016-pluggable-replication-per-tablet-raft.md) | Pluggable replication: per-tablet Raft (CP) alongside the AP data plane | Accepted |
| [0017](0017-per-tablet-raft-data-plane.md) | Per-tablet Raft data plane (leaderful, linearizable KV) | Accepted (§4/split superseded by 0028) |
| [0018](0018-cross-tablet-transactions.md) | Cross-tablet transactions on the CP plane (2PC over per-tablet Raft + HLC + MVCC) | Accepted — implemented (PR1: HLC + sim clock skew; PR2: HLC commit timestamps as the MVCC version + the range-seal design, replacing `version_floor`; PR2b: MVCC snapshot reads at a timestamp + the read-timestamp cache/logged read ceiling; PR3: single-participant transactions — the value envelope, the txn record/intent/resolve machinery through one Raft group; PR4: multi-participant 2PC across tablet Raft groups, the wire-level coordinator, foreign-intent resolution, and uncertainty-interval read restarts; PR5: in-doubt transaction recovery off a crashed coordinator + the per-node intent-resolver background task; PR6: the multi-tablet Elle serializability corpus + the protocol hardening fixes it found; PR7: atomic Dynamo `TransactWriteItems`, the new `TransactGetItems`, and `/admin/txns` observability; follow-up: apply-time write-key conditions, upgrading a write action's own `ConditionExpression` to full cross-node OCC; 2026-08-24 amendment: `ClientRequestToken` idempotency for `TransactWriteItems`; 2026-08-24 amendment: per-action `CancellationReasons`; 2026-08-26 amendments: issue #298 shapes A/B recovery-side fixes; 2026-08-27 amendment: issue #298 "deep shape A" residual closed — an ambiguous `cp_txn` outcome is retried internally and never recorded as a false `CANCELLED` in the `ClientRequestToken` idempotency cache — CQL transactional surface and manual txn-resolution admin actions deferred) |
| [0019](0019-cp-only-v1-defer-ap.md) | v1 ships the CP plane only; the leaderless AP data plane is deferred | Accepted |
| [0020](0020-admin-interface.md) | Admin / debug interface on a dedicated port (config, status, Raft + storage introspection, operator actions) | Accepted (2026-08-23 amendment: the long shot is **closed** — with CQL dropped by 0053, DynamoDB's wire cannot express a replication mode, so `ReplicationMode::Ap` is unreachable; `animus-consensus`, the Accord corpus and the `ReplicationMode` seam are deleted, superseding 0011 and 0014) |
| [0021](0021-web-dashboard.md) | Web dashboard over the admin JSON surface (observe + operator actions, static + self-contained) | Accepted |
| [0022](0022-hash-ring-partitioning.md) | Murmur3 partition token: hash the partition key (amends 0002) | Accepted |
| [0023](0023-table-scoped-tablets.md) | Table-scoped tablets on a per-table hash ring | Accepted |
| [0024](0024-drop-table-data-gc.md) | Drop-table data GC: `DropTableTablets` + a per-node reclaim loop (group stop, engine range erase, WAL file deletion) | Accepted |
| [0026](0026-multiplexed-node-stream-addressing.md) | Multiplexed `(node, stream)` addressing on the `Network` seam (retires the `Coresident` sibling-pool liveness cliff) | Accepted |
| [0027](0027-tracing-observability.md) | OpenTelemetry-compatible distributed tracing: `animusd`-only OTLP export, W3C trace-context propagation across a forwarded hop | Accepted |
| [0028](0028-shared-storage-single-command-split.md) | Shared per-node storage, control-plane-only tablet split (supersedes 0017 §4/split) | Superseded by [0050](0050-per-tablet-storage-copy-based-splits.md) (in full — per-tablet engines + the copy-based split workflow) |
| [0029](0029-replica-rebalancing.md) | Automatic tablet-replica rebalancing (amends 0005's reconciler, 0017's membership-change primitive) | Accepted |
| [0030](0030-online-cluster-growth.md) | Online cluster growth: admin add-member + heartbeat-driven activation (data-plane only; the control group stays static) | Accepted |
| [0031](0031-tablet-host-reconciler.md) | Per-node tablet-host reconciler + a metadata-applied watch primitive (consolidates 4 polling loops into one event-driven reconciler; amends 0028, 0029) | Accepted — implemented incrementally across PRs 1–6 |
| [0032](0032-seed-join-membership.md) | Seed/join membership: a replicated node address book, `animusd join`, and decommission (closes 0030's `client_route` gap; amends 0024, 0030) | Accepted — implemented (all three PRs: address book, `animusd join`, decommission) |
| [0033](0033-tablet-merge.md) | Tablet merge: an operator-driven, control-plane-only dual of split (amends 0029, extends 0031) | Superseded by [0044](0044-split-only-tablets.md) |
| [0034](0034-byte-based-auto-split.md) | Byte-based auto-split trigger: scoped byte estimate + byte-weighted median (amends 0002, 0028's auto-split loop) | Accepted |
| [0035](0035-control-plane-separate-deployment.md) | Control plane as a separate deployment: `animusd control` / `animusd data` over a `ControlHandle` seam (amends 0030, 0032) | Implemented — all 6 PRs shipped |
| [0036](0036-cluster-allocated-member-ids.md) | Cluster-allocated member ids: a monotonic `MetaCommand::AllocateNodeId` allocator, closing 0032's residual join-index race (amends 0032) | Superseded by [0040](0040-self-minted-string-node-ids.md) |
| [0037](0037-control-plane-membership-change.md) | Control-plane membership change: runtime grow/shrink/replace of control voters via `RaftNode::change_membership` + a new admin API/CLI (amends 0030, 0032) | Accepted — implemented |
| [0038](0038-control-metadata-system-keyspace.md) | Control-plane metadata backed by a per-node system-keyspace storage engine: `Metadata` is `DRIVER_APPLIED`, an async apply task replaces in-core apply (amends 0009, 0013, 0028, 0031, 0035) | Accepted — implemented across six PRs (key encoding, shadow mirror, the cutover, deployment-shape wiring, incremental `WatchMetadata` deltas, the system-table browse surface) |
| [0039](0039-control-metadata-system-tablet.md) | Control-plane metadata as a genuine data-plane tablet (ADR 0038's Option B, revisited): bootstrap circularity, a non-reconciled meta group, migration from the system keyspace, and why the scaling payoff is gated on ADR 0018 (amends 0038; depends on 0018) | Proposed — design-only, not scheduled |
| [0040](0040-self-minted-string-node-ids.md) | Self-minted string node identities + registration-CAS membership: one identity per node (Option B), an opaque validated `NodeId` string, `RegisterNode` CAS superseding ADR 0036's allocator, clean-break config/CLI, and an orphan-member auto-reclaim sweep (amends 0012, 0026, 0030, 0032, 0035, 0037, 0038; supersedes 0036) | Accepted — implemented (PR1–PR6, complete) |
| [0041](0041-materialized-secondary-indexes.md) | Materialized DynamoDB secondary indexes: a GSI is a hidden table maintained asynchronously by a derivative drain over a durable per-tablet change log (the log DynamoDB Streams will later consume), an LSI is colocated in the base tablet and atomic with the base write, plus per-row-kind storage scopes sharing one tablet's Raft group and key range (base rows, LSI rows, change log and footprints separated above the partition token, leaving the logical key layout untouched) and the deletion of the edge-local in-memory index (amends 0013, 0022, 0023, 0028, 0034; depends on 0018, 0024) | Accepted — implemented; ADR 0045 covers GSI create/backfill/drop on a populated table |
| [0042](0042-dynamo-streams.md) | DynamoDB Streams: semantics, API, and the change-log lifecycle (round 3) — the consumer contract (per-item ordering, eventual consistency via seal-and-serve, structural exactly-once), seal-epoch shards (`shardId-<tabletId>-<epoch>`, live lineage from day one), the `DynamoDBStreams_20120810` API surface, catalog-based label resolution, packed-HLC sequence numbers stable across sealing, non-expiring stateless iterators, leader-local hot reads with no `ReadIndex` barrier, the durability invariant + superset-slice rule, AWS-faithful post-disable readability (F12-b), and the tablet-merge stopgap (F1, since retired by 0044) (amends 0013, 0024, 0028, 0033/0044, 0034, 0035, 0041; depends on 0018, 0043) | Proposed |
| [0043](0043-stream-shard-subsystem.md) | In-place stream sealing and the `SegmentStore` (round 3, retitled from "The stream-shard subsystem") — a stream shard is a seal epoch of its source tablet's own existing change log, sealed by a per-node sealer arm on size/age triggers into a versioned segment; the `SegmentStore` trait (`animus-env`) with a cluster-replicated, K-durable default (`ClusterSegmentStore`) and an opt-in `FsSegmentStore`; the replicated segment catalog (`SealStreamShard`/`ExpireStreamShards`); split lineage with no data movement; and a segment-janitor loop merging retention with replica repair — replaces round 2's separate stream-shard tablets/copier/dedupe design entirely (amends 0013, 0024, 0028, 0033, 0034, 0035; depends on 0018, 0031, 0041, 0042) | Proposed |
| [0044](0044-split-only-tablets.md) | Tablets are split-only: tablet merge removed entirely — the DynamoDB never-merge precedent, the cross-group LWW witnessing crack PR1 found, the cheap-groups roadmap (quiescence, heartbeat amortization, asymmetric replicas, fleet-scale amortization), and the accepted dilution/shrink-in-place cost (supersedes 0033; amends 0002, 0018, 0029, 0034, 0042, 0043) | Accepted — implemented |
| [0045](0045-updatetable-gsi-backfill.md) | `UpdateTable` GSI add/drop on a populated table, with drain-driven backfill — an `IndexStatus` lifecycle (`Creating`/`Active`/`Deleting`), a leader-local backfill seeder arm reusing the ADR 0041 drain over synthetic per-partition change-log records (coverage, not correctness), a `stream_shards`-shaped completion catalog + control-leader aggregator, split-during-backfill (restart-from-scratch, geometrically bounded), the four-step drop cascade + `ClearBackfillCursor`, and the wire/`DescribeTable`/Console surfacing (amends 0013, 0041, 0044) | Accepted — implemented |
| [0046](0046-tablet-log-model.md) | The tablet log model: naming the shape ADR 0018/0028/0041/0042/0043/0045 each independently built — one Raft log per tablet materializing all colocated state at apply, async work as offset-tracked consumers/producers of the change-record stream, a split as a log cut with consumer offsets inherited from a frozen basis, authority at the serialization point — plus deciding the derived-state evaluation point (Fork U: evaluate-at-leader) and rejecting intent-staging of derived rows (amends 0018, 0028, 0041, 0042, 0043, 0045) | Accepted |
| [0047](0047-intra-node-port.md) | The intra-cluster port: splitting node-to-node `ClientRequest` traffic (`Forwarded`/`ProposeSchema`/`WatchMetadata`/`JoinInfo`/internal forwarding payloads) off the client port onto a new `intra` port — `ListenerKind`/`Surface`/the one exhaustive `surface_of` table, the parallel `intra_route`/`intra_leader_hint` machine-relay plumbing (vs. the untouched human-facing `leader_hint`), and `--seed` now naming the intra address — for the Kubernetes-operator deployment target (amends 0035, 0040) | Accepted — implemented |
| [0048](0048-cp-group-quiescence.md) | CP-group quiescence (phase 1): an idle Raft group stops ticking (timers/heartbeats/apply-poll) until woken by a write, a peer message, or the reconciler's proactive wake on a `Down` replica — the pure-core state machine, wake-on-demand + the `hot_read` scope-transition latch, quiesce vetoes, per-node sweeper skip, observability, and `--quiesce-after` (default-on) production wiring; closes ADR 0044's quiescence follow-up and narrows ADR 0043's `hot_read` residual to a strictly smaller, accepted sub-window (amends 0017; depends on 0009, 0012, 0031) | Accepted — implemented (2026-08-19 amendment: the external quiesce veto's stale-observation race, issue #302 — a log-index freshness stamp, `MIN_QUIESCE_AFTER`) |
| [0049](0049-universal-kind-write-path.md) | The universal kind-write path: every table routes every mutation through `KindBatch`, so every tablet has a change log unconditionally — image-less marker records for tables with no stream/GSI (the backfill-seeder shape), CQL writes migrated onto the kind path (no evaluate-at-leader hop; CQL CDC becomes near-free), a `TxnStage` stage-marker record, trim of consumer-less tables by the existing zero-terms rule, and a bench-gated plain-table write-path delta (amends 0006, 0018, 0041, 0042, 0043, 0046; the delta feed ADR 0050's split build consumes) | Accepted — implemented (Train A, rungs 1–5 + as-built amendment incl. the bench gate: ~1% plain-write delta after a confirm-cadence fix) |
| [0050](0050-per-tablet-storage-copy-based-splits.md) | Per-tablet physical storage and copy-based symmetric splits: each hosted tablet gets its own engine (keys `kind \|\| logical_key`, identity in the file namespace; `StorageScope`/fences/seals/basis/latch machinery dissolves, erase = file deletion) and a split becomes a DynamoDB-shaped background workflow — `BeginSplit` mints two `Building` children at placement-chosen homes, a driver drains the parent through the children's own Raft logs (`SeedBatch` bulk scan + change-log tail), a terminal `Freeze`, atomic `CutoverSplit` (children `Active`, parent removed + reclaimed, `split_lineage` frozen), sub-second retry-masked write blip, intents copied and resolves chasing (amends 0002, 0018, 0024, 0029, 0034, 0043, 0044, 0046, 0048; depends on 0049) | Accepted — implemented (Train B, rungs 1–8; supersedes 0028 in full; see the 2026-08-17 as-built amendment) |
| [0051](0051-dynamodb-ttl.md) | DynamoDB-style TTL: a `Clock::wall_now()`/`UnixMillis` wall-clock seam (the first calendar-time reading in the codebase, deterministic under `SimEnv` as a fixed epoch base plus virtual time, and skewable per node), a replicated `TtlSpec`/`SetTableTtl` in the schema catalog, AWS-faithful reads (an expired item stays visible until reaped, deliberately not filtered), a per-node leader-gated reaper deleting through the ADR 0049 kind-write path (so GSI/LSI/change-log/stream maintenance is inherited rather than reimplemented) under a delete conditional on the observed expiry, the five-year past-expiry safety window against the milliseconds-for-seconds mistake, read-without-waking quiescence handling, and the service `userIdentity` on TTL stream records (amends 0003, 0013, 0048; depends on 0042, 0043, 0046, 0049) | Accepted — implemented |
| [0052](0052-data-console-port.md) | The AnimusDB Data Console: a DynamoDB-shaped data app for application developers on its own new `console` port (seventh in the stride at the time), deliberately separate from the operator dashboard's admin port (no-auth, trusted-interface-only) and the DynamoDB wire port — bound on combined/data-only nodes, not control-only (hosts no tablet); this stack ships plumbing + a placeholder shell only, no JSON endpoints yet, auth a flagged prerequisite before wider exposure (amends 0021, 0035; depends on 0047) | Accepted — implemented; PR1–PR6 shipped, all three console screens (tables list, table detail, create-table form) exist, no follow-up PRs planned |
| [0053](0053-dynamodb-only-drop-cql.md) | v1 serves DynamoDB only: the CQL wire adapter (`animus-cql`) is deleted, along with its listener/port/dashboard panel and the CQL-only replicated-catalog surface (`Metadata::keyspaces`); the port stride shrinks from seven to six; the schema catalog's genuinely-shared union shape (clustering keys, non-DynamoDB `ColumnType`s) is kept as dead-cheap future-proofing rather than narrowed (supersedes 0006; amends 0047) | Accepted — implemented |
| [0054](0054-evaluate-writes-at-apply.md) | Evaluate a write inside the state machine, in commit order, instead of at the leader before the log: the entry carries the operation plus the schema slice it was accepted under, apply computes the new item and the stream images against the state it actually applies to, and results return through a leader-local payload beside the replicated outcome — retiring the OCC seatbelt, `rmw_lock`, and contention-caused write refusals (supersedes the evaluate-at-leader half of 0046; depends on 0053) | Proposed |
| [0055](0055-eventually-consistent-reads.md) | Cheap eventually-consistent reads: `ConsistentRead: false` — DynamoDB's wire default — stops being served by the linearizable ReadIndex path and is answered from **any** replica's own applied engine state, with no read barrier, no read-ceiling proposal, no read-timestamp-cache bump, no leadership requirement and no wake of a quiesced group; a purely local freshness gate (`stale_read_ready`: knows a leader, engine caught up to its known commit index) keeps a catching-up replica from fabricating absence, an unresolved transaction intent reads back one MVCC version to its last committed value instead of vanishing, routing prefers a local replica and otherwise takes one non-leader-chasing hop, and every step falls back to the untouched strong path — so followers finally serve reads and read-your-writes leaves the default read, exactly as DynamoDB defines it (amends 0017, 0041, 0048; depends on 0018, 0035, 0047, 0050; closes 0019's forward reference) | Accepted — implemented |
| [0056](0056-design-system.md) | One design system for the website and the in-product consoles: a shared `tokens.css` base (palette, the Space Grotesk/Martian Mono pairing, motion) with two skins on top — roomy for the site, dense for the consoles — OFL variable faces delivered per surface (Google Fonts on the public site, base64 `data:` URIs in the binary because a node-served console may have no egress), and a test that fails if the two token copies drift (amends 0021, 0052) | Accepted — implemented; **revised in place (2026-08-25 amendment)** from the original dark-first "Readout" direction (glow-means-live) to the light-first "Ledger" direction (keyline-means-live, one accent, ink rules/stamped pills/hatch fills/ink-plate terminals) — token names and the drift test unchanged |
| [0057](0057-sigv4-client-auth.md) | SigV4 authentication on the DynamoDB client edge: verify `AWS4-HMAC-SHA256` signatures against a static `access_key_id → secret` map from node config (optional `dynamo_auth` section / `--dynamo-auth` flag, default-off) — a pure verifier module in `animus-dynamo` (first crypto deps: `sha2`/`hmac`; vendored official AWS test vectors), thin middleware at the `animusd` DynamoDB HTTP edge, AWS-faithful `com.amazon.coral.service#…` error mapping, ±5-min `X-Amz-Date` skew via `wall_now()`; client port only — intra/admin/console postures unchanged; explicitly not IAM: no policies, no rotation, no multi-tenancy, no TLS (amends 0047, 0052; depends on 0051, 0053) | Accepted — implemented |
| [0058](0058-learner-replicas-in-place-split.md) | Learner replicas and in-place tablet split: a non-voting `RaftCore` membership class (Train 1, ships alone — closes the today-a-new-replica-dilutes-quorum-while-catching-up gap on both planes) plus a single-entry atomic in-place split (Train 2) that adds a parent's placement-chosen homes as learners, forks two child engines via SSTable-level clone at one `SplitTablet` apply (the same ADR 0050 copy-kinds filter, reusing ordinary Raft catch-up instead of `SeedBatch`/tail passes/a final-image re-scan), proposed to replace ADR 0050's build/freeze/cutover workflow while keeping its per-tablet private engines and `split_lineage` fork F9 — the layout-scoping argument that ruled this out under ADR 0028's shared engine no longer applies under ADR 0050's own private-engine layout (amends 0017 §4, 0028, 0029, 0030, 0031, 0044, 0046; depends on 0009, 0018, 0049, 0050) | Accepted — implemented (rungs 1–4 layers 1–2; in-place is now `SplitMode`'s default, `--split-mode copy` still selects the original ADR 0050 workflow; rung 4's remaining layer — deleting the copy workflow — pending; **Train 2 Stage 1/2's fork F5 superseded by [0062](0062-fork-first-split-directed-placing.md)**) |
| [0059](0059-backup-restore.md) | Backup and restore: on-demand backups + point-in-time recovery as one internal snapshots-plus-change-log mechanism — a separately configured `SegmentStore` handle (`--backup-store`) and object namespace, a manifest + chunked BASE/LSI/FOOTPRINT-only data objects (never CHANGE/CURSOR), a backup catalog keyed by backup id (never table name, and deliberately outliving the source table), per-tablet leader-side capture reading through intent resolution, the backup-vs-split race closed by re-planning onto `split_lineage` rather than a GC veto, restore always to a new table via `CreateTableSchema` + `propose_seed_batch` with GSIs rebuilt through the existing ADR 0045 backfill lifecycle (never captured), and PITR as a fifth change-log consumer arm sealing continuously alongside periodic base snapshots (35-day default retention); S3 export/import and the S3 `SegmentStore` backend explicitly deferred (amends 0013, 0024, 0043, 0049, 0050, 0045, 0051; depends on 0043, 0049, 0050, 0038, 0044) | Accepted — implemented; Train 1 (catalog, capture, wire surface, janitor), Train 2 (restore), and Train 3 (PITR) all shipped — the backup/restore/PITR feature train is complete |
| [0060](0060-kubernetes-operator.md) | The Kubernetes operator: hostname-native addressing (`animusd` groundwork — SIGTERM parity, hostname-capable `--seed`/peer book/route books, an advertise-vs-bind address split so a StatefulSet pod's stable per-pod DNS name, not its churning IP, is what `RegisterNode`'s CAS keys on) plus a new `animus-operator` crate (kube-rs, an `AnimusCluster` CRD, pure desired-child-object builders, static generated bootstrap config, ADR 0032-driven scale-down) — fulfills the Kubernetes-operator deployment target root `CLAUDE.md`/ADR 0047 already named (amends 0047, 0032, 0035, 0040, 0057; depends on 0003, 0020, 0030, 0055) | Accepted — implemented; `crates/animus-operator` + `deploy/operator` + a `kind` e2e (`scripts/e2e-kind.sh`) exist; the operator's own container image is still unpublished |
| [0061](0061-testability-node-crate-simulator.md) | Testability: push the `Env` seam up one layer into a new `E: Env`-generic `animus-node` crate (with no `tokio::net`/`ProdEnv` in its manifest, so determinism is compiler-enforced rather than review-enforced), leaving `animusd` the binary/config/listeners/lifecycle; a byte-level seam under the hand-rolled HTTP edges so the DynamoDB wire path becomes sim-drivable; simulator fault-vocabulary extensions (per-link network config, duplication, wire corruption, delay distributions, process-pause, clock drift, ENOSPC, fsync-acked-but-lost) plus the failure minimization ADR 0003 promised and never built; a shared corpus harness collapsing `name_seed`/`seeds_from_env`/`seed_expand` duplicated across 11 corpora; `disallowed-methods` lints for `tokio::spawn`/`std::time`/`thread_rng`; and pure-logic extraction + property tests across placement, tablet, storage, cp-data and dynamo (amends 0003, 0020, 0026, 0035, 0052; depends on 0003, 0009, 0017) | Proposed — plan of record; Phases A–E |
| [0062](0062-fork-first-split-directed-placing.md) | Fork-first in-place split with directed placing: an in-place split's children fork directly onto the parent's own current replica set (no learner recruitment, no cross-cluster catch-up gate before the fork), with their real final placement decided once, at `CutoverSplit`'s own apply, as a pure function of already-agreed `Metadata` (`Metadata::split_placing`) and then driven there by the very same `reconfigure_step`/`CasTabletReplicas` convergence machinery that already drives ordinary rebalance — a new `reconcile_loop` third phase and a `MarkSplitPlacingDone` completion signal, split-complete reported (never gated) once placed — supersedes ADR 0058's fork-F5 half of Train 2 Stage 1/2 only, keeping its single-entry mint, materialization, G4 crash contract, and cutover guard rails (amends 0029, 0031; supersedes 0050 fork F5 for the in-place path; depends on 0005, 0029, 0058 Train 1/Stage 3-4, 0059) | Accepted — implemented (rungs 1–7, e2e + bench re-validated; a suspected `reconfigure_step` two-of-three-replica oscillation, issue #513, investigated and closed as not reproducible — Placing's reach is not limited) |
| [0063](0063-order-preserving-number-keys.md) | Order-preserving key encoding for `N` (number) key attributes: replaces `AttributeValue::key_bytes()`'s raw-decimal-text `N` encoding with a canonical sign + exponent + digit-run layout (inverted for negatives, terminator byte load-bearing for prefix runs), applied at that single choke point so bytewise stored-key order equals DynamoDB numeric order — fixing `ScanIndexForward` across magnitudes/signs while the existing numeric filter (`SortKeyCondition::matches`/`matches_raw`, issue #373) stays authoritative; `bigdecimal` differential proptest is the correctness oracle; reaches base/GSI/LSI keys and an `N` partition key's token input (re-placing rows across the ring), inherited automatically by streams' `ChangeRecord.base_sk` and backup/restore objects; `animus-tablet` holds no `N` encoding to mirror (amends 0022, 0023; no migration owed per root `CLAUDE.md`) | Accepted |
| [0064](0064-tls-on-every-port.md) | TLS on every port: mutual TLS on the internal ports (the raw Raft wire, and the intra `ClientRequest` relay port) authenticating cluster membership at the transport, server-only TLS on the client/admin/console ports (SigV4, ADR 0057, remains the client-side identity story), a file-based `TlsConfig{cert_path,key_path,ca_path}` defaulting to off with an all-TLS-or-all-plain rule on the internal wire, and a `MaybeTlsStream` transport wrapper in `animus-env` chosen over boxing (hot path) and a Cargo feature (would bifurcate builds) — `ring` crypto provider, already in the graph via `kube` (amends 0047, 0057, 0060) | Accepted — implemented; all four commits landed (mutual TLS inside `ProdEnv`; every `animusd`/`animus-cli` listener and dialer; the Kubernetes operator's cert-manager/Secret wiring; the closing website/roadmap update), config-gated and default off throughout |
| [0065](0065-per-table-throttling.md) | Per-table throttling in DynamoDB capacity units, enforced per tablet: a table's provisioned RCU/WCU divided evenly across its current tablet count (re-derived live, no cross-node coordination), a token bucket per tablet (300s DynamoDB-style burst window, sharing `RequestRateTracker`'s `RateSample`/`env.now()`/`retain_existing` shape, W-09) checked before propose on the write side and before the engine read on the read side — never inside Raft apply (ADR 0054) — a two-layer `ClusterSettings` default / per-table `TableSchema.throughput` config surface (`MetaCommand::SetTableThroughput`, modelled on `SetTableTtl`), and AWS-faithful wire behavior (`ProvisionedThroughputExceededException`, `UnprocessedItems`/`UnprocessedKeys`, a `ThrottlingError` transaction-cancellation reason) — throttling without a billing meter, deliberately (amends 0013, 0034; depends on 0018, 0049, 0054, 0055) | Accepted |
| [0066](0066-sigv4-hardening.md) | SigV4 hardening: replaces ADR 0057's static credential map with a replicated `credentials: BTreeMap<AccessKeyId, CredentialRow>` catalog in `Metadata` (verified locally on every node, data-only included) — `PutCredential`/`RotateCredential`/`RevokeCredential` `MetaCommand`s modelled on the backup catalog's row shape, a dual-secret rotation grace window read through `env.wall_now()`, and a minimal per-key `Policy{tables, ops}` allow list (six `OpClass`es, table match by name/prefix/all) enforced once at the receiving node after signature verification, denying with `AccessDeniedException`; static `dynamo_auth`/`--dynamo-auth` keys become a bootstrap layer the catalog shadows on a shared id; deliberately not IAM — no policy language, conditions, STS, or per-item authorization, per `docs/roadmap.md` §6 (amends 0057; depends on 0013, 0020, 0038, 0051, 0059, 0064) | Accepted |
